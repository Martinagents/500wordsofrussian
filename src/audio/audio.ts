export async function playRussian(text:string,audioUrl?:string){if(audioUrl){new Audio(audioUrl).play().catch(()=>{});return}if(!('speechSynthesis'in window))throw new Error('Speech synthesis unavailable');const u=new SpeechSynthesisUtterance(text);u.lang='ru-RU';const voices=speechSynthesis.getVoices();u.voice=voices.find(v=>v.lang.toLowerCase().startsWith('ru'))||null;speechSynthesis.cancel();speechSynthesis.speak(u)}
